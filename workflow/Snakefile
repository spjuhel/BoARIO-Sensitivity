# Main entrypoint of the workflow.
# Please follow the best practices:
# https://snakemake.readthedocs.io/en/stable/snakefiles/best_practices.html,
# in particular regarding the standardized folder structure mentioned there.
conda:
    "envs/global.yaml"

from snakemake.utils import min_version
from snakemake.utils import Paramspace
import pandas as pd
import yaml

min_version("8.0.0")

#### Config file ####
configfile: "config/config.yaml"

module mriot_tools_snake:
    snakefile:
        # here, it is also possible to provide a plain raw URL like "https://github.com/snakemake-workflows/dna-seq-gatk-variant-calling/raw/v2.0.1/workflow/Snakefile"
        "/home/sjuhel/Repos/BoARIO-MRIOT-Tools/workflow/Snakefile"
    config:
        config["data-mriot"]
    prefix:
        config["data-mriot"]["prefix"]

include: "rules/prepare_simspace.smk"
include: "rules/simulations.smk"
include: "rules/treat_results.smk"

#include: "rules/report.smk" # Yet to be correctly implemented


use rule * from mriot_tools_snake exclude all_mriot

#### wildcard constraints ####
wildcard_constraints:
    year=r"\d\d\d\d",
#    aggregation=r"full_exio3compat|74_sectors",
    scope=r"general|local",
    focus=r"all_sim|no_absurd",
